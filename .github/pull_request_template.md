<!-- 제목 형식: <type>(<scope>): <한글 제목>   예) feat(payment): 멱등키 선점으로 중복 승인 차단 -->

Closes #

## 무엇을 했는가

<!-- 변경의 요약. diff를 다시 쓰지 말고 의도를 쓴다. -->

## 왜 이렇게 했는가

<!-- 고려한 대안과 기각 이유. 되돌리기 어려운 결정이면 ADR로도 남긴다. -->

## 증명

<!--
이 PR이 무엇을 보장하는지, 어떤 테스트가 그것을 증명하는지.
P0x 라벨이 붙은 이슈라면 이 칸이 비어 있을 수 없다.

예) IdempotencyConcurrencyTest — 동시 100요청 시 PG Mock 호출 1회 검증
-->

## 체크리스트

- [ ] 이슈의 완료 기준을 전부 만족한다
- [ ] `./gradlew build` 통과 (테스트 포함)
- [ ] ArchUnit 모듈 경계 테스트 통과
- [ ] `P0x` 이슈라면, 그 문제를 증명하는 테스트가 이 PR에 있다
- [ ] 새 설정값·환경변수는 README 또는 compose 파일에 반영했다
- [ ] 설계 판단이 바뀌었다면 설계서 또는 ADR을 함께 수정했다
